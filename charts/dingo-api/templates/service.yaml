apiVersion: v1
kind: Service
metadata:
  name: {{ include "dingo-api.fullname" . }}
  labels:
    {{- include "dingo-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dingo-api.selectorLabels" . | nindent 4 }}
