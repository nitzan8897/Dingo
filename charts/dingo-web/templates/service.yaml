apiVersion: v1
kind: Service
metadata:
  name: {{ include "dingo-web.fullname" . }}
  labels:
    {{- include "dingo-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dingo-web.selectorLabels" . | nindent 4 }}
